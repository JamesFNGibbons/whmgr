<div class='page-header'>
	<h2>Select Database To Export</h2>
</div>
<div class='well'>
	This wizard will export the contents and structure of the
	selected database.
	<a class='btn btn-primary pull-right' href='/databases/exports'>
		Go back
	</a>
</div>
<div class='row'>
	<div class='col-md-5'>
		{{#unless databases}}
			<div class='alert alert-danger'>
				There are no databases to export.
			</div>
		{{/unless}}
		<form method='post'>
			<div class='form-group'>
				<label>Select Database</label>
				<select name='database' class='form-control'>
					{{#each databases}}
						<option value='{{database}}'>{{database}}</option>
					{{/each}}
				</select>
			</div>
			<div class='form-group'>
				{{#if databases}}
					<input type='submit' class='btn btn-primary pull-right' value='Export Data'>
				{{/if}}
			</div>
		</form>
	</div>
</div>